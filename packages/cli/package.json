{
  "name": "@dep-guardian/cli",
  "version": "0.1.0",
  "description": "CLI for dep-guardian",
  "type": "module",
  "bin": {
    "dep-guardian": "./dist/index.js",
    "dg": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@dep-guardian/core": "*",
    "commander": "^12.0.0",
    "chalk": "^5.3.0",
    "ora": "^8.0.0",
    "inquirer": "^9.0.0"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.0"
  }
}
